# URL del backend en producción. OBLIGATORIA — si falta, el frontend
# apunta a localhost:8000 y todas las llamadas fallan en el navegador.
#
# Setear en Vercel: Settings → Environment Variables (Production + Preview).
# No usar barra final.
NEXT_PUBLIC_API_URL=https://api.elige-tuplan.cl
